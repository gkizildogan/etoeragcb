# Copy to deploy/.env. Do not commit the populated file.
PUBLIC_DOMAIN=rag.example.com
ACME_EMAIL=admin@example.com
TLS_MODE=public
CADDYFILE=./Caddyfile
LOG_LEVEL=INFO
MODEL_CACHE_HOST_PATH=../model-cache/huggingface/hub
SECRETS_GID=1000
BACKUP_DESTINATION=gdrive://folder/replace-with-restricted-folder-id
BACKUP_RETENTION=30
INACTIVE_VERSION_RETENTION_DAYS=35
EPHEMERAL_RECORD_RETENTION_DAYS=7
GC_BACKUP_MAX_AGE_HOURS=36
MAINTENANCE_ENABLED=true

# At least 32 random characters. This protects only the internal SearXNG service.
SEARXNG_SECRET=replace-with-a-random-value-at-least-32-characters

# Optional overrides; relative paths are resolved from deploy/.
POSTGRES_PASSWORD_FILE=./secrets/postgres_password
DATABASE_URL_FILE=./secrets/database_url
JWT_SECRET_FILE=./secrets/jwt_secret
SIGNING_SECRET_FILE=./secrets/signing_secret
BACKUP_ENCRYPTION_KEY_FILE=./secrets/backup_encryption_key
SMTP_PASSWORD_FILE=./secrets/smtp_password
